<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
		  <%= render partial: 'layouts/errors', locals: { obj:@book } %>
  		<h1>Editing Book</h1>
  		<%= render partial: 'books/form', locals: { jonsoku: @book, btn:@btn } %>
			

  		<%= link_to 'Show', @book %> |
  		<%= link_to 'Back', books_path, class: "back" %>
  </div>
</div>